# TODO — готово к работе

> Задачи проработаны, приоритизированы и готовы к выполнению. Отсортированы по приоритету.

## Высокий приоритет
<!-- Критически важные задачи, блокирующие другие -->

**D-14: Реализация идемпотентности в inventory/add-items и проверка пакетного добавления предметов**
**Роль:** Backend Developer
**Приоритет:** Высокий  
**Статус:** [ ] Готов к выполнению

**Описание:**
Необходимо обеспечить идемпотентность операций добавления предметов в inventory-service и проверить корректность пакетного добавления предметов в production-service.

**🔍 Проблема:**
В методе `api/inventory/add-items` отсутствует идемпотентность - можно дважды добавить одинаковые предметы с одинаковыми `operation_type` и `operation_id`, что приводит к дублированию предметов в инвентаре.

**📋 План реализации:**

**Фаза 1: Анализ и проектирование**
- [ ] **Анализ схемы БД**: Изучить таблицу `inventory.operations` и индексы
- [ ] **Определить уникальность**: Создать составной уникальный индекс на (user_id, operation_id, operation_type, item_id, collection_id, quality_level_id)
- [ ] **Стратегия обработки дубликатов**: Решить что делать при повторном запросе (игнорировать/ошибка/возврат существующих операций)

**Фаза 2: Реализация идемпотентности в inventory-service**
- [ ] **Создать миграцию**: Добавить уникальный индекс для предотвращения дубликатов
- [ ] **Обновить AddItems**: Обработка нарушения уникального ключа
- [ ] **Возврат существующих операций**: При дубликате возвращать уже созданные операции вместо ошибки
- [ ] **Логирование**: Добавить логи для отслеживания попыток дублирования

**Фаза 3: Проверка production-service**
- [ ] **Анализ кода**: Проверить как production-service вызывает AddItems
- [ ] **Пакетное добавление**: Убедиться что одинаковые предметы добавляются одним запросом, а не последовательными
- [ ] **Группировка предметов**: Если нужно, реализовать группировку одинаковых предметов перед отправкой в inventory
- [ ] **Обработка ошибок**: Проверить корректность обработки ответов от inventory-service

**Фаза 4: Тестирование**
- [ ] **Unit тесты**: Тесты идемпотентности AddItems с различными сценариями
- [ ] **Integration тесты**: Тесты полного цикла production → inventory
- [ ] **Нагрузочные тесты**: Проверка поведения при конкурентных запросах
- [ ] **Regression тесты**: Убедиться что существующая функциональность не сломана

**💡 Примеры сценариев для тестирования:**

1. **Повторный claim**: Пользователь дважды нажимает кнопку claim задания
2. **Сетевые проблемы**: Повторная отправка запроса при таймауте
3. **Одинаковые предметы**: Рецепт выдает несколько одинаковых предметов
4. **Конкурентные запросы**: Одновременные запросы с одинаковым operation_id

**⚠️ Критические моменты:**
- **Atomicity**: Операции должны быть атомарными - либо все предметы добавляются, либо ни одного
- **Performance**: Уникальный индекс не должен существенно замедлить операции
- **Backwards compatibility**: Существующие клиенты должны продолжать работать
- **Error handling**: Четкие коды ошибок для различных сценариев

**🕒 Оценка времени:** 8-12 часов разработки + 4-6 часов тестирования

**Критерии готовности:**
- [ ] **Идемпотентность**: Повторные запросы с одинаковыми параметрами не создают дубликаты
- [ ] **Пакетная обработка**: Production-service группирует одинаковые предметы в один запрос
- [ ] **Производительность**: Время ответа AddItems не увеличилось более чем на 10%
- [ ] **Надежность**: Система корректно обрабатывает конкурентные запросы
- [ ] **Мониторинг**: Добавлены метрики для отслеживания попыток дублирования
- [ ] **Документация**: Обновлена спецификация API с описанием идемпотентности

**Файлы для изучения:**
- `services/inventory-service/internal/handlers/inventory.go` - AddItems handler
- `services/inventory-service/internal/storage/inventory_storage.go` - операции с БД
- `services/production-service/internal/service/task_service.go:processTaskClaim` - вызов AddItems
- `docs/specs/inventory-service.md` - текущая спецификация API

---

**D-13: Полная реализация функциональности лимитов производственных рецептов**
**Роль:** Backend Developer
**Приоритет:** Высокий  
**Статус:** [ ] Готов к выполнению

**Описание:**
Необходимо завершить реализацию полной функциональности лимитов рецептов согласно спецификации. Текущая реализация адаптирована под упрощенную схему БД, но для полноценной работы требуется расширение до целевой архитектуры с поддержкой классификаторов и гибких ограничений.

**🔍 Текущее состояние:**
- **Упрощенная реализация**: Работает с базовой схемой `production.recipe_limits` (id, recipe_id, limit_type, max_uses, created_at)
- **Ограничения**: Только лимиты на выполнение рецептов, без поддержки лимитов на конкретные предметы
- **Отсутствует**: Связи с классификаторами через limit_type_id, limit_object_id, target_item_id

**🎯 Целевая архитектура (согласно спецификации):**
```sql
CREATE TABLE production.recipe_limits (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    recipe_id UUID NOT NULL REFERENCES production.recipes(id),
    limit_type_id UUID NOT NULL REFERENCES core.limit_types(id),
    limit_object_id UUID NOT NULL REFERENCES core.limit_objects(id), 
    target_item_id UUID REFERENCES core.items(id),
    max_uses INTEGER NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**📋 План реализации:**

**Фаза 1: Миграция базы данных**
- [ ] **Создать миграцию добавления полей**: limit_type_id, limit_object_id, target_item_id
- [ ] **Обратная совместимость**: Временно сохранить поле limit_type для переходного периода
- [ ] **Заполнить новые поля**: Маппинг существующих limit_type в соответствующие UUID из классификаторов
- [ ] **Создать индексы**: На новые внешние ключи для оптимизации запросов

**Фаза 2: Обновление моделей данных**
- [ ] **Расширить RecipeLimit**: Добавить поля LimitTypeID, LimitObjectID, TargetItemID
- [ ] **Обновить UserRecipeLimit**: Добавить поддержку target_item_id в API ответах
- [ ] **Обратная совместимость**: Поддержать оба формата (старый string + новый UUID) на период миграции

**Фаза 3: Бизнес-логика и репозиторий**
- [ ] **Расширить GetRecipeUsageStats**: Добавить фильтрацию по target_item_id для лимитов на конкретные предметы
- [ ] **Обновить CheckRecipeLimits**: Реализация проверки лимитов с учетом объекта ограничения и целевого предмета
- [ ] **Интеграция с классификаторами**: Использование ClassifierRepository для преобразования кодов в UUID
- [ ] **Оптимизация запросов**: Efficient JOIN запросы с классификаторами

**Фаза 4: API и интеграция**
- [ ] **Обновить API ответы**: Включить информацию о target_item_id в UserRecipeLimit
- [ ] **Расширить фильтрацию**: Поддержка фильтрации рецептов по доступности с учетом лимитов на предметы  
- [ ] **Валидация**: Проверка корректности лимитов при создании производственных заданий

**Фаза 5: Тестирование и документация**
- [ ] **Unit тесты**: Покрытие новой логики лимитов с различными сценариями
- [ ] **Integration тесты**: Тесты полного цикла с реальными классификаторами
- [ ] **Performance тесты**: Проверка производительности запросов с JOIN по классификаторам
- [ ] **Обновить спецификацию**: Документирование новых возможностей API

**💡 Примеры использования после реализации:**

1. **Лимит на выполнение рецепта**: "Рецепт можно выполнить максимум 5 раз в день"
   - limit_type: "daily", limit_object: "recipe_execution", target_item_id: NULL

2. **Лимит на получение предмета**: "Из этого рецепта можно получить максимум 10 железных мечей в неделю"  
   - limit_type: "weekly", limit_object: "item_reward", target_item_id: UUID железного меча

3. **Лимит на сундуки**: "Можно открыть максимум 3 золотых сундука в месяц"
   - limit_type: "monthly", limit_object: "item_reward", target_item_id: UUID золотого сундука

**⚠️ Риски и зависимости:**
- **Классификаторы**: Требуется наличие записей в core.limit_types и core.limit_objects
- **Миграция данных**: Необходим план отката при проблемах с миграцией
- **Производительность**: JOIN запросы могут повлиять на скорость API
- **Совместимость**: Фронтенд должен поддержать новые поля в API

**🕒 Оценка времени:** 16-20 часов разработки + 4-6 часов тестирования

**Критерии готовности:**
- [ ] **Полная схема БД**: Все поля согласно спецификации реализованы и заполнены
- [ ] **Гибкие лимиты**: Поддержка лимитов как на выполнение рецептов, так и на получение конкретных предметов
- [ ] **API консистентность**: UserRecipeLimit содержит всю необходимую информацию
- [ ] **Производительность**: Время ответа GET /production/recipes не увеличилось более чем на 20%
- [ ] **Обратная совместимость**: Старые клиенты продолжают работать без изменений
- [ ] **Тестовое покрытие**: >80% покрытие новой функциональности
- [ ] **Документация**: Обновлена спецификация и примеры использования

---

**D-12: Исправление HMAC валидации Telegram WebApp в production**
**Роль:** Backend Developer
**Приоритет:** Высокий  
**Статус:** [ ] Требует расследования и исправления

**Описание:**
Auth Service успешно аутентифицирует пользователей Telegram WebApp, но HMAC подпись не совпадает с ожидаемой. Временно HMAC валидация отключена для development среды, но это критично для production безопасности.

**🔍 Детали проблемы:**
- **Полученный hash**: `12620fff246f641d134e88ff4bb2e1ee2c58d8d9b677be8d0754b750ea453b89`  
- **Вычисленный hash**: `22781e9fc836aa91dab8348748721df21f81055d47ed8d3ac9f6516a2de150d5`
- **Бот токен**: `7986845757:AAG1L4612Tag4DtXyPMSMzDr_7pZHFdgTxM` (@SLCWDevDimBot)
- **Статус**: Все остальные проверки (auth_date, user data, структура) проходят успешно

**Возможные причины:**
1. Фронтенд инициализирован с другим ботом/токеном
2. Модификация initData после получения от Telegram  
3. Неправильная обработка URL encoding/JSON экранирования
4. Использование устаревшего Telegram WebApp API

**Критерии выполнения:**
- [ ] **Расследование**: Определить точную причину несовпадения HMAC
- [ ] **Проверка бота**: Убедиться что фронтенд использует правильный бот
- [ ] **Тестирование с оригинальными данными**: Проверить с реальными Telegram initData  
- [ ] **Исправление алгоритма**: При необходимости скорректировать валидацию
- [ ] **Включение строгой проверки**: Убрать временный bypass для production
- [ ] **Regression тесты**: Убедиться что исправление не ломает существующие тесты

**Временное решение:**
Аутентификация работает с предупреждением в логах `"Continuing despite HMAC validation failure for development"`

**Файлы для изучения:**
- `services/auth-service/internal/services/telegram.go:77-82` - место временного bypass
- Frontend инициализация Telegram WebApp
- Логи auth-service для анализа приходящих данных

---

**D-7: Внедрение метрик и мониторинга в Production Service**
**Роль:** DevOps/Backend Developer
**Приоритет:** Средний
**Статус:** [ ] Готов к выполнению

**Описание:**
Интеграция мониторинга и метрик для наблюдения за работой production-service по аналогии с auth-service и inventory-service.

**Критерии выполнения:**
- [ ] **Prometheus метрики**: HTTP метрики, бизнес-метрики производства, метрики модификаторов
- [ ] **Grafana дашборд**: Создание дашборда `production-service-metrics.json`
- [ ] **Алерты**: Правила алертов в `deploy/monitoring/alerts.yml`
- [ ] **Интеграция middleware**: Подключение метрик в HTTP handlers и бизнес-логику
- [ ] **Мониторинг производственных операций**: Метрики времени производства, успешности claim операций
- [ ] **Dependency health**: Мониторинг статуса inventory-service и user-service
- [ ] **Тестирование**: Проверка сбора метрик в dev окружении

**Файлы для изучения:**
- `services/auth-service/pkg/metrics/` - пример реализации
- `deploy/monitoring/grafana/dashboards/auth-service-metrics.json` - шаблон дашборда
- `docs/specs/production-service.md`

---

**D-20: Обновление атрибутов ItemDetailsResponse в inventory-service**
**Роль:** Backend Developer
**Приоритет:** Средний  
**Статус:** [x] Выполнено

**Описание:**
Необходимо обновить схему ответа эндпоинта `POST /items/details`:
- Добавить обязательный атрибут `item_class` (пример значения: `"chests"`).
- Переименовать атрибут `code` в `item_type` для унификации с остальными схемами.
- Обновить OpenAPI спецификацию (`docs/specs/inventory-service-openapi.yml`) и документацию (`docs/specs/inventory-service.md`).
- Привести модели, бизнес-логику и тесты `inventory-service` в соответствие новой схеме.

**Критерии выполнения:**
- [ ] OpenAPI спецификация и Markdown-документация обновлены.
- [ ] Модели DTO и структура ответа хэндлера скорректированы.
- [ ] Добавлены/обновлены unit-тесты на новый формат.
- [ ] Обновлён фронтенд-SDK (если затрагивает).
- [ ] Поддержка обратной совместимости — старое поле `code` не возвращается.

**Оценка времени:** 4-6 часов разработки + 2 часа тестирования

---

**D-15: Унифицировать публичные эндпоинты inventory-service через nginx**
**Роль:** Backend Developer / DevOps
**Приоритет:** Средний  
**Статус:** [ ] Готов к планированию

**Описание:**
В настоящее время inventory-service обслуживает две версии публичных маршрутов: `/inventory/*` (новые) и `/api/inventory/*` (legacy). Для обратной совместимости были добавлены алиас-роуты в самом сервисе, что усложняет кодовую базу.

**Цель:** Перейти к единственному набору эндпоинтов `/inventory/*`, обеспечив совместимость через конфигурацию `nginx` в `api-gateway`. Требуется настроить rewrite/proxy_pass так, чтобы все старые обращения `/api/inventory/*` корректно проксировались на новые маршруты *без* необходимости поддержки алиасов в Go-коде.

**Критерии готовности:**
- [ ] В `inventory-service` удалены временные алиас-роуты `/api/inventory/*`.
- [ ] В `deploy/dev/api-gateway/nginx.conf` настроен rewrite с сохранением аутентификации/заголовков.
- [ ] Все смежные сервисы (deck-game, production и пр.) продолжают работать без изменений кода.
- [ ] Интеграционные тесты: открытие сундуков, выдача предметов, получение инвентаря — проходят через новый маршрут.
- [ ] Документация (`docs/specs/inventory-service.md`, READMEs) обновлена.

**Оценка времени:** 3-4 часа (настройка + тестирование)

---

## D-Deck-006: Метрики и логирование
**Описание:** Добавить Prometheus-метрики (`dgs_http_requests_total`, `dgs_daily_craft_total`, …) и structured-logging (slog) аналогично inventory-service.
**Приоритет:** Средний
**Оценка:** S
**Зависимости:** D-Deck-005
**Критерии готовности:**
- Endpoints `/metrics` экспортирует новые счётчики `dgs_http_requests_total`, `dgs_daily_craft_total`, `dgs_http_latency_seconds`
- Метрики зарегистрированы через общий `metrics` пакет и протестированы с `prometheus/testutil`
- README и Grafana dashboard обновлены и описывают новые метрики
- Во всех слоях (handlers, service, storage) добавлены structured-логи уровней INFO, WARN, ERROR
- Unit-тесты покрывают ≥ 80 % новой логики; сборка и линт проходят без ошибок
- CI pipeline «test» и «lint» зелёные, Docker-образ собирается

**Ресурсы:**
- docs/specs/deck-game-service.md (метрики)
- services/inventory-service/internal/metrics/* (пример)

---
## D-Deck-007: Dockerfile & Dev Compose
**Описание:** Создать Dockerfile и обновить `deploy/dev/docker-compose.yml`, добавив контейнер `deck-game-service` (порт 8080 и 8090). Сеть и переменные окружения настроены как у inventory.
**Приоритет:** Средний
**Оценка:** S
**Зависимости:** D-Deck-006
**Критерии готовности:**
- `docker build .` завершает сборку без ошибок; образ запускается с ожидаемыми портами
- `docker compose up` поднимает сервис без ошибок; endpoint `/health` отвечает 200 OK ≤ 5 сек.
- Контейнер подключён к сети `backend`; переменные окружения переданы через `.env.dev`
- Ports 8080/8090 проброшены; internal API недоступен извне (network policy / expose -)
- README обновлён инструкциями сборки и запуска; CI job `docker-build` зелёный
- Unit-тесты и линт проходят

**Ресурсы:**
- deploy/dev/docker-compose.yml (пример inventory)
- services/inventory-service/Dockerfile


---
## Низкий приоритет
<!-- Задачи для будущих итераций -->

Эндпоинт для получения локализованной информации о предметах

**Критерии перехода в TODO:**
- [ ] Задача четко сформулирована
- [ ] Определены critical path критерии готовности  
- [ ] Проведена оценка трудозатрат
- [ ] Нет блокирующих зависимостей
- [ ] Назначен ответственный (опционально) 